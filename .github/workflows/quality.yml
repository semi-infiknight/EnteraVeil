name: Quality
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.1
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
      - name: Install workspace deps
        run: pnpm install --frozen-lockfile
      - name: Typecheck storefront
        run: pnpm --filter ./apps/storefront typecheck
      - name: Typecheck medusa
        run: pnpm --filter ./apps/medusa typecheck
